# Supabase — Project Settings → API
# Ces variables sont embarquées dans le bundle envoyé au navigateur (préfixe VITE_).
# N'y mets QUE la clé anon/publishable, jamais la clé service_role.
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
